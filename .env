# ✅ Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=newpassword  
DB_NAME=ridesharing

# ✅ MySQL Root User (Optional for Local Development)
MYSQL_ROOT_USER=root
MYSQL_ROOT_PASSWORD=newpassword 

# ✅ Docker Configuration (If Using Containers)
DB_CONTAINER=db

# Session Secret
SESSION_SECRET=UniRideSharing_SecureSecret123

# Docker Configuration (Only used if NODE_ENV=docker)
DOCKER_DB_HOST=db
DOCKER_DB_PORT=3306

# Set this to 'docker' if using Docker, or leave empty for local development
# NODE_ENV=docker
